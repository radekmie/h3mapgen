#include "structures.h"

void bfs(struct* map M, int** res, int sx, int sy)
{
    int qx[M->n];
    int qy[M->m];
    
    for (int i = 0; i < M->n; i++)
        for (int j = 0; j < M->m; j++)
            res[i][j] = oo;
    
    
    
}
